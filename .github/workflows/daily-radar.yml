name: Legacy Daily AI Radar (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled legacy workflow
        run: |
          echo "This legacy GitHub Actions generator is disabled."
          echo "Daily publishing is handled by the Codex local automation, which generates the multi-section report."
